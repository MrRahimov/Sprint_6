# Sprint_6 — UI-тесты «Яндекс.Самокат»

## Стек
Python 3, pytest, Selenium 4, Firefox, allure-pytest

## Установка
```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -U pip
python -m pip install -r requirements.txt

